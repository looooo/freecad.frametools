from setuptools import setup

setup(name='freecad_frame',
      version='0.0',
      packages=['freecad',
                'freecad.modules',
                'freecad.modules.frame_tools'],
      include_package_data=True)
